name: united_mania_flutter
description: The best Man United news app.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  get: ^4.3.8
  http: ^0.13.3
  url_launcher: ^6.0.9
  infinite_scroll_pagination: ^4.0.0
  cached_network_image: ^3.1.0
  cupertino_icons: ^1.0.2
  fluttertoast: ^8.2.6

dev_dependencies:
  flutter_test:
    sdk: flutter


flutter:
  uses-material-design: true
  assets:
    - assets/